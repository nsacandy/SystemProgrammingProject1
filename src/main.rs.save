use std::fs;
use std::io;

fn main() -> io::Result<()> {
    let contents = fs::read_to_string(file_path)?;

    // Print the file content
    println!("File Contents:\n{}", contents);
    
    Ok(())
}fn main() {
    println!("Hello, world!");
}
